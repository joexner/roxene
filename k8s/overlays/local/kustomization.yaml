# Small footprint for a local kind cluster
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: worker-memory-patch.yaml

replicas:
  - name: worker
    count: 2
